experiment:
  name: "Qwen2.5_Series_Stag_Hunt"
  
  # List of games to run experiments with
  games:
    - "Stag_Hunt"
  
  # List of models to run experiments with
  models: 
    - "/data/home/jjl7137/huggingface_models/Qwen/Qwen2.5-0.5B-Instruct"
    - "/data/home/jjl7137/huggingface_models/Qwen/Qwen2.5-1.5B-Instruct"
    - "/data/home/jjl7137/huggingface_models/Qwen/Qwen2.5-3B-Instruct"
    - "/data/home/jjl7137/huggingface_models/Qwen/Qwen2.5-7B-Instruct"
    - "/data/home/jjl7137/huggingface_models/Qwen/Qwen2.5-14B-Instruct"
    # Add more models as needed
  
  game:
    name: "Stag_Hunt"  # Default game if needed
    # Additional game parameters
    previous_actions_length: 0
    
  data:
    generate: false # generate game data if true
    num_scenarios: 36
    batch_size: 4
  
  llm:
    model_name: 'meta-llama/Llama-3.1-8B-Instruct'  # Default model if needed
    # Additional parameters to override or extend the base config
    generation_config:
      temperature: 0.7
      do_sample: true
      top_p: 0.95
      max_new_tokens: 440

  batch_size: 500

  # Template for system message with placeholders
  system_message_template: "Remember you are Alice. What is your option? Choose one option shown above."
  
  repeat: 1

  emotions:
    - "anger"
    - "happiness"
    - "sadness"
    - "disgust"
    - "fear"
    - "surprise"

  intensity:
    - 1.5

  run_sanity_check: false

  output:
    base_dir: "results/Qwen2.5_Series_Stag_Hunt"
    save_plots: true
    plot_format: "png" 